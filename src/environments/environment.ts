// Environment configuration for the chatbot UI
export const environment = {
  production: false,
  apiUrl: 'http://localhost:5001/api/query', // Update this to your Python backend URL
  apiTimeout: 30000, // 30 seconds
  enableDebugLogging: true
};
